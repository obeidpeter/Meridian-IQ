auto-install-peers=false
strict-peer-dependencies=false
# Playwright browsers are installed explicitly where needed (CI runs
# `playwright install chromium`; the E2E harness accepts a preinstalled
# executable) — never as a ~400MB side effect of `pnpm install`.
playwright_skip_browser_download=1
