// The bell's display vocabulary lives in the workspace package (shared with
// the SME app) so the two feeds cannot drift.
export {
  badgeText,
  channelBadgeClasses,
  channelLabel,
  markReadTimestamp,
  NOTIFICATION_FEED_LIMIT,
  relativeTime,
} from "@workspace/format";
