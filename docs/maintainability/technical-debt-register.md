# Technical Debt Register

This register is ordered by change risk and user impact. Counts should be
refreshed with `pnpm run maintainability:report` and
`pnpm run complexity:report` before each quarterly planning cycle.

| Priority | Debt                                   | Evidence                                                                                            | Recommended action                                                                                    | Exit criterion                                                                    |
| -------- | -------------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| P1       | Oversized role pages                   | Met in R120 for the page directories and extended in R126: the shells themselves now hand state to one-bag hooks and render through sibling components (SME dashboard 117 → 13, SME invoice detail 107 → 11, buyer Confirmations 71 → 7, console pages, the four other SME pages, six Expo screens), and the two landing shells that sat outside `pages/` are split (App.tsx 2,009 → 97 lines, InvoiceRoom.tsx 1,572 → 117). No hand-written page or shell exceeds 1,400 lines | Hold the line: a new page or module joins the sibling-directory shape, and the complexity ratchet catches a shell that regrows | Met |
| P1       | Console/SME bundle size                | Vite reports about 1.17 MB and 0.94 MB minified JS                                                  | Route-level lazy imports, measured vendor chunking, preload only primary path                         | Initial route JS below agreed performance budget with no loading regressions      |
| P1       | Coverage visibility                    | Broad suites exist but no merged per-package coverage threshold (stage one landed in R118: the api-server suite runs under V8 coverage in CI with per-area line and branch floors for auth, tenancy, money, the Clerk gateway and the pipeline in `coverage-floors.json`, ratcheted by `test:coverage:write`; stage two in R121 gave web-ui, the console and the SME app the same floors through Vitest's V8 provider; the buyer portal, landing and mobile suites still run plain) | Add V8 coverage in stages around auth, RLS, money, Clerk gateway, pipeline; then the web packages and a changed-code threshold | Critical packages publish coverage and ratcheted changed-code threshold           |
| P2       | Exact UI/config duplication            | Met in R126: the 17 shadcn primitives, the `cn` helper, `QueryError` and the shared Today-setup suite body moved into `lib/web-ui` behind per-app re-export shims, taking the exact-duplicate group count from 21 to 2 (the two remaining are `main.tsx` entry files and two `vitest.config.ts` files, both intentionally local) | Keep new cross-app primitives in `lib/web-ui`; leave intentional per-app wrappers local | Met: duplicate group count ratcheted 21 → 2 with no app dependency leakage |
| P2       | Historical formatting debt             | Met in R124: one mechanical Prettier pass over the 588 code files that differed, with `.prettierignore` keeping the generated clients, build output and the non-code formats (Markdown, YAML, JSON, HTML, CSS, SVG) out; `pnpm run format:check` runs in the check chain and CI, so a change must format what it touches | Keep the check green; a later pass may bring CSS and markup under the same check | Repository format check passes with no mixed behavior churn           |
| P2       | Dependency migration backlog           | Major updates pending across OpenAI (6 to 7), Expo (54 to 57, with the mobile package's Zod-3-only `zod-validation-error`), Vite 8 and Vitest 5 (shipped after R119; Zod moved to 4 in R119, Pino to 10 with pino-http 11 and thread-stream 4 in R123, one ecosystem per round) | Upgrade one ecosystem at a time with changelog review and full CI                                     | Supported versions, zero unignored high/critical advisories                       |
| P2       | Source-posture tests parse text        | Security tripwires intentionally inspect route source                                               | Gradually replace with exported policy metadata and HTTP integration assertions                       | Critical posture proved behaviorally; source checks retained only where necessary |
| P3       | Mobile build requires hosting metadata | Expo artifact is environment-bound                                                                  | Add documented local preview and reproducible CI packaging profile                                    | Mobile build can run in a clean release runner with explicit inputs               |
| P3       | Complexity ratchet                     | Met in R122 and tightened in R126: `complexity-baseline.json` now records 82 functions over 20 (maximum 37, down from 114 and 117) keyed by file and function label; `pnpm run complexity:check` in the check chain and CI fails on a new hotspot or growth and advises when an entry can tighten; `complexity:write` moves the baseline down | Bring the remaining hotspots down one at a time and tighten the baseline in the same change; keep the threshold at 20 | CI complexity ratchet active and owned                                 |

## Rules

- Do not reduce a metric by moving generated/build output into source or by
  disabling a check.
- Do not consolidate UI code whose interaction or accessibility semantics
  differ across roles.
- Security, data isolation, idempotency, and append-only evidence outrank line
  count or abstraction purity.
- Every P1 item requires an owner and a rollback plan before implementation.
