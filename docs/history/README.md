# History

Dated working records of past rounds, kept for provenance. They describe the
state at the time they were written and are not maintained; the live
procedures are `docs/operations.md`, `docs/platform.md` and the decision log in
`docs/architecture.md` plus `docs/adr/`.

| Folder | What it holds |
| --- | --- |
| `2026-09-r198/` | The R198 integrity, recovery and verified-release round: implementation notes, the maintenance forward-fix plan, the runtime evidence and state contracts, the operations and frontend contracts, and the worker reliability notes. The OpenAPI fragments that accompanied them were removed — the contract is `lib/api-spec/openapi.yaml`. |
