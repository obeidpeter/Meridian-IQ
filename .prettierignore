# Generated clients: regenerated by `api-spec run codegen`, never edited.
lib/api-zod/src/generated
lib/api-client-react/src/generated

# Build, tool and test output (the .gitignore entries also apply).
dist
build
coverage
static-build
out-tsc
test-results
tmp
release
.expo
artifacts/api-server/data
pnpm-lock.yaml
*.min.js

# Content formats kept as authored: the docs gates parse the Markdown
# tables, the ops scripts write the JSON baselines and floors in their own
# shape, and markup and styles are outside the R124 code pass.
*.md
*.yaml
*.yml
*.json
*.html
*.css
*.svg

# orval appends its own single-quoted export lines to these two entry files
# on every codegen run and only recognises them as written; formatting them
# would make each run append duplicates.
lib/api-zod/src/index.ts
lib/api-client-react/src/index.ts
